/**
 * @param {number} m
 * @param {number} n
 * @return {number}
 */
var rangeBitwiseAnd = function(m, n) {
    // 勉强可以通过，但是不是最优解
    // if(!m) return 0
    // for(let i = m + 1; i <= n; i++) {
    //     m = m & i
    // }
    // return m
};