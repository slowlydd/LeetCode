/**
 * @param {number[]} height
 * @return {number}
 */
var trap = function(height) {
  if(height.length < 3) {
    return 0;
  } else {
    let open = true;
    let p1 = 0;
    let p2 = 0;
    for(let i = 0; i < height.length; i++)
  }
};